pipeline {
    agent any 

         stages {
             stage('Instalar dependencias'){
                steps{
                    sh 'npm install cypress --save-dev'
                }
            }     


        }
    }